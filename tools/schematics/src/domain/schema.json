{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsDomain",
  "title": "Domain Schematic Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Domain name",
      "x-prompt": "What is the name of the domain?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
