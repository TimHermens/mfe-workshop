{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsApi",
  "title": "Api Schematic Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "domain": {
      "type": "string",
      "aliases": ["d"],
      "description": "The domain this api belongs to",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What domain does this api belong to?"
    }
  },
  "required": ["domain"]
}
