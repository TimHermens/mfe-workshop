{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsUtil",
  "title": "Util Schematic Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "domain": {
      "type": "string",
      "aliases": ["d"],
      "description": "The domain this util belongs to",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What domain does this util belong to?"
    }
  },
  "required": ["domain"]
}
