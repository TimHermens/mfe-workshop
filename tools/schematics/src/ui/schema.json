{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsFeature",
  "title": "Feature Schematic Schema",
  "cli": "nx",
  "type": "object",
  "properties": {
    "domain": {
      "type": "string",
      "aliases": ["d"],
      "description": "The domain this ui library belongs to",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What domain does this ui library belong to?"
    }
  },
  "required": ["domain"]
}
